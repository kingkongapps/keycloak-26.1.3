CREATE TABLE sample (
  id              INT       NOT NULL AUTO_INCREMENT,
  name            VARCHAR(50)   NULL,
  addr            VARCHAR(500)  NULL,
  cell_phone      VARCHAR(500)  NULL,
  PRIMARY KEY (id)
);
